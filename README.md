# ShowAPI NodeJS SDK 使用方法

## 安装
    npm install sdk-showapi

## 使用
    require('showapi').api(url, [
                {key: '', value: ''},
                {key: '', value: ''}
            ], function(err, res) {
            });
